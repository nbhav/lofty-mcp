{
  "resource": "leads",
  "tag": "Leads",
  "sourceSpec": "specs/openapi.json",
  "authNote": "Every operation's OpenAPI doc says 'Authorization: Bearer [access_token]' \u2014 that is WRONG for the personal-access API key this project uses. Empirically confirmed working: 'Authorization: token <key>'. Bearer returns HTTP 400 {\"code\":200058,\"message\":\"User in token does not exist.\"}. See specs/rest/index.json.",
  "operationCount": 12,
  "operations": [
    {
      "operationId": "updateContacts",
      "method": "PUT",
      "path": "/v1.0/brokermint/lead",
      "summary": "Brokermint \u2014 Sync Lead",
      "description": "Callback endpoint for Brokermint to push contact updates into Lofty. Not intended for general external callers.\n\nThe target contact must have been previously linked to a Brokermint record through the Lofty UI's Brokermint Integration. Without this link the response body contains a non-zero code despite HTTP 200.",
      "parameters": [
        {
          "name": "Content-Type",
          "in": "header",
          "required": true,
          "type": "string",
          "format": null,
          "description": "application/json"
        }
      ],
      "requestBody": {
        "required": true,
        "schemaRef": "#/components/schemas/Contacts",
        "inlineType": null
      }
    },
    {
      "operationId": "list_4",
      "method": "GET",
      "path": "/v1.0/leads",
      "summary": "List Leads",
      "description": "Returns a page of leads matching the supplied filters with pagination metadata.\n\nNotes:\n- limit must be in [1, 100]; otherwise 400 LIMIT_NOT_VALID.\n- Use offset for offset-based pagination or scrollId for cursor-based pagination.\n- Multiple filters are combined with AND logic; multi-value filters within a single parameter (e.g. allTags, anyTags) apply their documented AND / OR semantics.",
      "parameters": [
        {
          "name": "stage",
          "in": "query",
          "required": false,
          "type": "string",
          "format": null,
          "description": "Search for lead by pipeline stage names."
        },
        {
          "name": "source",
          "in": "query",
          "required": false,
          "type": "string",
          "format": null,
          "description": "Search for lead by source name."
        },
        {
          "name": "phone",
          "in": "query",
          "required": false,
          "type": "string",
          "format": null,
          "description": "Precise search for lead by phone number.\nExample: 3479033333"
        },
        {
          "name": "email",
          "in": "query",
          "required": false,
          "type": "string",
          "format": null,
          "description": "Precise search for lead by email address."
        },
        {
          "name": "assignedUserId",
          "in": "query",
          "required": false,
          "type": "integer",
          "format": "int64",
          "description": "Search for contacts by user that is assigned to them."
        },
        {
          "name": "contacted",
          "in": "query",
          "required": false,
          "type": "boolean",
          "format": null,
          "description": "Search for contacts by if they have or have not been contacted."
        },
        {
          "name": "groups",
          "in": "query",
          "required": false,
          "type": "string",
          "format": null,
          "description": "Search for lead by group names. deprecated, use segments instead."
        },
        {
          "name": "segments",
          "in": "query",
          "required": false,
          "type": "string",
          "format": null,
          "description": "Search for lead by segment names."
        },
        {
          "name": "allTags",
          "in": "query",
          "required": false,
          "type": "string",
          "format": null,
          "description": "Search for lead with all tag names."
        },
        {
          "name": "anyTags",
          "in": "query",
          "required": false,
          "type": "string",
          "format": null,
          "description": "Search for lead with any tag names."
        },
        {
          "name": "groupIds",
          "in": "query",
          "required": false,
          "type": "integer",
          "format": "int64",
          "description": "Search for lead by office ids."
        },
        {
          "name": "querySubGroup",
          "in": "query",
          "required": false,
          "type": "boolean",
          "format": null,
          "description": "Query sub group leads or not"
        },
        {
          "name": "scrollId",
          "in": "query",
          "required": false,
          "type": "string",
          "format": null,
          "description": "Scroll ID for the search. You can then use the scroll ID to retrieve the next batch of results for the request."
        },
        {
          "name": "offset",
          "in": "query",
          "required": false,
          "type": "integer",
          "format": null,
          "description": "Start index of the responded leads"
        },
        {
          "name": "limit",
          "in": "query",
          "required": false,
          "type": "integer",
          "format": null,
          "description": "Quantitative limitation of responded leads, range: [1, 100]"
        },
        {
          "name": "sort",
          "in": "query",
          "required": false,
          "type": "string",
          "format": null,
          "description": "The sort order can be one of the following: Default, LastContact, LastCall, LastEmail, LastActivity, CreateTime, Inquries, DeleteTime, AssignTime, Score, Activity, Communication, HasDrip, HasAlert, Task, Lender, ActivitySearch, ActivityViews, ActivityFavorites, DripStatus, DripOpenRate, AlertFrequency, AlertOpenRate, SCOREV2 or SmartPlanStatus"
        },
        {
          "name": "desc",
          "in": "query",
          "required": false,
          "type": "boolean",
          "format": null,
          "description": "Descending sort(true) or ascending sort(false)"
        },
        {
          "name": "languages",
          "in": "query",
          "required": false,
          "type": "string",
          "format": null,
          "description": "Search for lead with any languages (abbreviation)"
        },
        {
          "name": "preciseSearchFlag",
          "in": "query",
          "required": false,
          "type": "boolean",
          "format": null,
          "description": "Whether to precise search for leads based on email or phone"
        },
        {
          "name": "roleAssigneeFilters",
          "in": "query",
          "required": false,
          "type": "string",
          "format": null,
          "description": "[{\"roleName\":\"Lead assign role name\",\"assigneeToIds\":[111,222]}]"
        },
        {
          "name": "key",
          "in": "query",
          "required": false,
          "type": "string",
          "format": null,
          "description": "Search by name, phone or email"
        },
        {
          "name": "otherFilters",
          "in": "query",
          "required": false,
          "type": "string",
          "format": null,
          "description": null
        },
        {
          "name": "returnFields",
          "in": "query",
          "required": false,
          "type": "string",
          "format": null,
          "description": null
        }
      ],
      "requestBody": null
    },
    {
      "operationId": "add_3",
      "method": "POST",
      "path": "/v1.0/leads",
      "summary": "Create Lead",
      "description": "## Lead Assignment & Routing\n\n**Assignment priority:**\n1. If `assignedUserId` is provided, the lead is directly assigned to that agent. No routing rules are applied.\n2. If `assignedUserId` is empty or omitted, the lead goes through automatic lead routing based on the configured routing rules.\n\n**Routing rule scope** (when routing is triggered):\n- `assignGroupId = -1` \u2192 Apply company-level routing rules.\n- `assignGroupId = <officeId>` \u2192 Apply routing rules for the specified office.\n- `assignGroupId` not set \u2192 Default routing rules will be applied according to the lead\u2019s ownership scope.\n\n**Routing location matching:**\n- For Seller (1) type leads, routing is determined by the `property` field (property address/location).\n- For all other lead types (Buyer, Renter, etc.), routing is determined by `inquiry.locations[]` (interested areas).\n\n## Lead Ownership\n\nControls the visibility scope of the lead:\n- `ownershipScope = TEAM` \u2192 Account-wide: visible to all users in this Lofty client account.\n- `ownershipScope = OFFICE` \u2192 Visible to a specific office. Must provide `ownershipId` with the office ID.\n- `ownershipScope = PERSONAL` \u2192 Visible only to the assigned agent. Must provide `ownershipId` with the agent's user ID.\n- If not specified, defaults to PERSONAL scope owned by the API caller.\n\n> Note: `ownershipScope = TEAM` refers to the entire client account (the organization), not the Lofty \"Team add-on\" product.\n\n## Eventual consistency\n\nPersistence is asynchronous. The lead ID is returned immediately, but subsequent GET /v1.0/leads/{leadId} and similar read endpoints may briefly return 404 right after create. Follow-up writes (PUT, POST inquiry, POST property, POST transaction) are unaffected.\n\n## Property block\n\nThe optional `property` block is persisted together with the lead:\n- Omit it entirely if you don't need to attach an address \u2014 the lead is still created.\n- If provided, at least one of `city`, `state`, `zipCode`, `streetAddress` must be non-blank; otherwise the request is rejected with **200057 PROPERTY_ADDRESS_EMPTY** and no lead is created.",
      "parameters": [
        {
          "name": "Content-Type",
          "in": "header",
          "required": true,
          "type": "string",
          "format": null,
          "description": "application/json"
        }
      ],
      "requestBody": {
        "required": true,
        "schemaRef": "#/components/schemas/LeadRequest",
        "inlineType": null
      }
    },
    {
      "operationId": "getAssigneeInfo",
      "method": "POST",
      "path": "/v1.0/leads/assignee",
      "summary": "Preview Routing",
      "description": "Returns the assignees that would be selected by routing rules for a hypothetical lead matching the supplied name, email and other attributes. Useful for previewing routing before creating the lead.\n\nNotes:\n- firstName and email are required.\n- The response lists one entry per role matched by routing.",
      "parameters": [
        {
          "name": "Content-Type",
          "in": "header",
          "required": true,
          "type": "string",
          "format": null,
          "description": "application/json"
        }
      ],
      "requestBody": {
        "required": true,
        "schemaRef": "#/components/schemas/LeadAssigneeRequest",
        "inlineType": null
      }
    },
    {
      "operationId": "trash",
      "method": "DELETE",
      "path": "/v1.0/leads/{leadId}",
      "summary": "Delete Lead",
      "description": "Moves a lead to the trash. A reason is required and is stored for audit. The response body is empty.",
      "parameters": [
        {
          "name": "leadId",
          "in": "path",
          "required": true,
          "type": "integer",
          "format": "int64",
          "description": "ID of the lead to trash."
        },
        {
          "name": "reason",
          "in": "query",
          "required": true,
          "type": "string",
          "format": null,
          "description": "Free-text reason recorded with the trash operation."
        },
        {
          "name": "Content-Type",
          "in": "header",
          "required": true,
          "type": "string",
          "format": null,
          "description": "application/json"
        }
      ],
      "requestBody": null
    },
    {
      "operationId": "get",
      "method": "GET",
      "path": "/v1.0/leads/{leadId}",
      "summary": "Get Lead",
      "description": "Returns a single lead by ID. The response is wrapped under the 'lead' key.\n\nNotes:\n- By default, trashed leads are not returned. Pass withTrash=true to include them.\n- Caller must have access to the lead; otherwise 404 LEAD_NOT_EXIST.",
      "parameters": [
        {
          "name": "leadId",
          "in": "path",
          "required": true,
          "type": "integer",
          "format": "int64",
          "description": "ID of the lead to retrieve."
        },
        {
          "name": "withTrash",
          "in": "query",
          "required": false,
          "type": "boolean",
          "format": null,
          "description": "When true, include trashed leads in the lookup."
        }
      ],
      "requestBody": null
    },
    {
      "operationId": "update_1",
      "method": "PUT",
      "path": "/v1.0/leads/{leadId}",
      "summary": "Update Lead",
      "description": "Updates an existing lead. Caller must have manage permission on the lead.\n\n## Property block\n\nIf the request body carries a `property` block, it is persisted alongside the lead update with the same semantics as `POST /v1.0/leads/{leadId}/property`:\n\n- **`property.id` omitted or `<= 0`** \u2014 merge with an existing property by address; if no match, create a new property record. (Prior versions of this endpoint rejected this case with `UPDATE_LEAD_PROPERTY_FAILED`; that restriction has been lifted.)\n- **`property.id > 0`** \u2014 update the named property in place. The property must belong to this lead (its `leadUserId` must match); otherwise the request is rejected with 200108 LEAD_PROPERTY_NOT_EXIST.\n\nValidation:\n- When a `property` block is supplied, at least one of `city`, `state`, `zipCode`, `streetAddress` must be non-blank; otherwise 200057 PROPERTY_ADDRESS_EMPTY is returned (the lead update is also aborted).",
      "parameters": [
        {
          "name": "leadId",
          "in": "path",
          "required": true,
          "type": "integer",
          "format": "int64",
          "description": "ID of the lead to update."
        },
        {
          "name": "Content-Type",
          "in": "header",
          "required": true,
          "type": "string",
          "format": null,
          "description": "application/json"
        }
      ],
      "requestBody": {
        "required": true,
        "schemaRef": "#/components/schemas/EditLeadRequest",
        "inlineType": null
      }
    },
    {
      "operationId": "listActivities_1",
      "method": "GET",
      "path": "/v1.0/leads/{leadId}/activities",
      "summary": "Lead Activities",
      "description": "Returns the lead's site-tracked activities (browse / search / favorite / showing request / submission) across tracking types 1, 2, 3, 23 and 39, merged and sorted by created time descending.\n\nNotes:\n- Caller must have manage permission on the lead; otherwise 404 LEAD_NOT_EXIST.\n- Page size is fixed at 100; use curPage to step through pages.",
      "parameters": [
        {
          "name": "leadId",
          "in": "path",
          "required": true,
          "type": "integer",
          "format": "int64",
          "description": "ID of the lead whose activities to return."
        },
        {
          "name": "curPage",
          "in": "query",
          "required": false,
          "type": "integer",
          "format": "int32",
          "description": "Zero-based page index; page size is fixed at 100."
        }
      ],
      "requestBody": null
    },
    {
      "operationId": "activity",
      "method": "POST",
      "path": "/v1.0/leads/{leadId}/activity",
      "summary": "Add activity",
      "description": "Add activity to a lead.",
      "parameters": [
        {
          "name": "leadId",
          "in": "path",
          "required": true,
          "type": "integer",
          "format": "int64",
          "description": null
        },
        {
          "name": "Content-Type",
          "in": "header",
          "required": true,
          "type": "string",
          "format": null,
          "description": "application/json"
        }
      ],
      "requestBody": {
        "required": true,
        "schemaRef": "#/components/schemas/LeadActivity",
        "inlineType": null
      }
    },
    {
      "operationId": "assign",
      "method": "POST",
      "path": "/v1.0/leads/{leadId}/assignment",
      "summary": "Assign Lead",
      "description": "Place assignee to a lead.",
      "parameters": [
        {
          "name": "leadId",
          "in": "path",
          "required": true,
          "type": "integer",
          "format": "int64",
          "description": null
        },
        {
          "name": "Content-Type",
          "in": "header",
          "required": true,
          "type": "string",
          "format": null,
          "description": "application/json"
        }
      ],
      "requestBody": {
        "required": true,
        "schemaRef": null,
        "inlineType": "array"
      }
    },
    {
      "operationId": "updateInquiry",
      "method": "POST",
      "path": "/v1.0/leads/{leadId}/inquiry",
      "summary": "Add Inquiry",
      "description": "Place an inquiry for a lead.",
      "parameters": [
        {
          "name": "leadId",
          "in": "path",
          "required": true,
          "type": "integer",
          "format": "int64",
          "description": null
        },
        {
          "name": "Content-Type",
          "in": "header",
          "required": true,
          "type": "string",
          "format": null,
          "description": "application/json"
        }
      ],
      "requestBody": {
        "required": true,
        "schemaRef": "#/components/schemas/LeadInquiry",
        "inlineType": null
      }
    },
    {
      "operationId": "updateProperty",
      "method": "POST",
      "path": "/v1.0/leads/{leadId}/property",
      "summary": "Update Property Info",
      "description": "Attach a property (or mailing address) to the specified lead. The behaviour depends on the `property.id` field in the request body:\n\n- **`id` omitted or `<= 0`** \u2014 the service first tries to merge with an existing property on the lead that has the same street/city/state/zip; if no match is found, a new property record is created.\n- **`id > 0`** \u2014 the named property is updated in place. The property must belong to this lead (its `leadUserId` must match the lead's); otherwise the request is rejected with 200108 LEAD_PROPERTY_NOT_EXIST.\n\nValidation:\n- At least one of `city`, `state`, `zipCode`, `streetAddress` must be non-blank; otherwise 200057 PROPERTY_ADDRESS_EMPTY is returned.\n- Caller must have manage permission on the lead.",
      "parameters": [
        {
          "name": "leadId",
          "in": "path",
          "required": true,
          "type": "integer",
          "format": "int64",
          "description": null
        },
        {
          "name": "Content-Type",
          "in": "header",
          "required": true,
          "type": "string",
          "format": null,
          "description": "application/json"
        }
      ],
      "requestBody": {
        "required": true,
        "schemaRef": "#/components/schemas/LeadProperty",
        "inlineType": null
      }
    }
  ]
}
