{
  "resource": "listing-v2",
  "tag": "Listing V2",
  "sourceSpec": "specs/openapi.json",
  "authNote": "Every operation's OpenAPI doc says 'Authorization: Bearer [access_token]' \u2014 that is WRONG for the personal-access API key this project uses. Empirically confirmed working: 'Authorization: token <key>'. Bearer returns HTTP 400 {\"code\":200058,\"message\":\"User in token does not exist.\"}. See specs/rest/index.json.",
  "operationCount": 1,
  "operations": [
    {
      "operationId": "searchListings",
      "method": "POST",
      "path": "/v2.0/listings/search",
      "summary": "Search Listings",
      "description": "Search listings by location keywords and optional filters (price, bedrooms, bathrooms, sqft, property type, listing status, purchase type).",
      "parameters": [
        {
          "name": "Content-Type",
          "in": "header",
          "required": true,
          "type": "string",
          "format": null,
          "description": "application/json"
        }
      ],
      "requestBody": {
        "required": true,
        "schemaRef": "#/components/schemas/ListingQueryRequest",
        "inlineType": null
      }
    }
  ]
}
